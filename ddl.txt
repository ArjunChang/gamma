CREATE TABLE `ae_staking_assets` (
  `id` int(11) NOT NULL,
  `asset_staked` varchar(10) COLLATE ascii_bin NOT NULL,
  `validator_name` varchar(45) COLLATE ascii_bin NOT NULL,
  `validator_code` varchar(45) COLLATE ascii_bin NOT NULL,
  `blockchain_id` varchar(45) COLLATE ascii_bin NOT NULL,
  `token` varchar(10) COLLATE ascii_bin NOT NULL,
  `token_status` enum('pending','unstaked','staked') COLLATE ascii_bin NOT NULL,
  `start_date` datetime NOT NULL,
  `end_date` datetime NOT NULL,
  `duration_staked_loan` decimal(32,16) NOT NULL,
  `trading_entity` varchar(45) COLLATE ascii_bin NOT NULL,
  `rewards_earned_till_date` decimal(32,16) DEFAULT NULL,
  `rebate_earned_till_date` decimal(32,16) DEFAULT NULL,
  `apy` decimal(32,16) DEFAULT NULL,
  `loan_id` int(11) DEFAULT NULL,
  `rewards` decimal(32,16) DEFAULT NULL,
  `rewards_validator` decimal(32,16) DEFAULT NULL,
  `net_rewards_falconx` decimal(32,16) DEFAULT NULL,
  `transaction_hash` varchar(45) COLLATE ascii_bin DEFAULT NULL,
  `t_create` datetime NOT NULL DEFAULT CURRENT_TIMESTAMP,
  `t_delete` datetime NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  `t_update` datetime DEFAULT NULL,
  PRIMARY KEY (`id`)
) ENGINE=InnoDB DEFAULT CHARSET=ascii COLLATE=ascii_bin;


CREATE TABLE `ae_staking_liabilities` (
  `loan_id` int(11) NOT NULL,
  `counterparty_name` varchar(45) COLLATE ascii_bin NOT NULL,
  `counterparty_code` varchar(45) COLLATE ascii_bin NOT NULL,
  `token` varchar(10) COLLATE ascii_bin NOT NULL,
  `start_date` datetime NOT NULL,
  `end_time` datetime NOT NULL,
  `duration_of_loan` decimal(32,16) DEFAULT NULL,
  `entity` varchar(45) COLLATE ascii_bin NOT NULL,
  `rewards_dist_ratio` decimal(32,16) DEFAULT NULL,
  `gross_rewards` decimal(32,16) DEFAULT NULL,
  `net_rewards` decimal(32,16) DEFAULT NULL,
  `attachment` varchar(45) COLLATE ascii_bin DEFAULT NULL,
  `transaction_hash` varchar(45) COLLATE ascii_bin DEFAULT NULL,
  `input_by` varchar(45) COLLATE ascii_bin DEFAULT NULL,
  `t_create` datetime NOT NULL DEFAULT CURRENT_TIMESTAMP,
  `t_update` datetime DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  `t_delete` datetime DEFAULT NULL,
  PRIMARY KEY (`loan_id`)
) ENGINE=InnoDB DEFAULT CHARSET=ascii COLLATE=ascii_bin;


CREATE TABLE `ae_structured_product_credit` (
  `sp` int(11) NOT NULL AUTO_INCREMENT,
  `counterparty_name` varchar(45) NOT NULL,
  `counterparty_code` varchar(45) NOT NULL,
  `token` varchar(10) NOT NULL,
  `token_quantity` decimal(32,16) NOT NULL,
  `notional_value` decimal(32,16) NOT NULL,
  `fixed_interest_rate` decimal(32,16) NOT NULL,
  `interest_token` varchar(10) NOT NULL,
  `start_date` datetime NOT NULL,
  `end_date` datetime NOT NULL,
  `duration_of_loan` decimal(32,16) NOT NULL,
  `funding_rate_source` varchar(45) NOT NULL,
  `eod_price_of_token` decimal(32,16) NOT NULL,
  `notional_based_on_eop` decimal(32,16) NOT NULL,
  `funding_rate` decimal(32,16) NOT NULL,
  `entity` varchar(45) NOT NULL,
  `funding_fee_amount_per_day` decimal(32,16) NOT NULL,
  `funding_fee_cumulative` decimal(32,16) NOT NULL,
  `funding_fee_outstanding` decimal(32,16) NOT NULL,
  `collateral` decimal(32,16) NOT NULL,
  `attachment` varchar(45) NOT NULL,
  `transaction_hash_loan` varchar(45) NOT NULL,
  `transaction_hash_interest_payments` varchar(45) NOT NULL,
  `entry_details` varchar(10) NOT NULL,
  `transaction_entry_input_time` datetime NOT NULL,
  `source` enum('manual','system') DEFAULT NULL,
  `modification_time` datetime NOT NULL,
  `record_modified_by` varchar(10) NOT NULL,
  `t_create` datetime NOT NULL DEFAULT CURRENT_TIMESTAMP,
  `t_update` datetime DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  `t_delete` datetime DEFAULT NULL,
  PRIMARY KEY (`sp`)
) ENGINE=InnoDB DEFAULT CHARSET=latin1;

CREATE TABLE `ae_aggregated_liquidity_trades` (
  `id` int(11) NOT NULL AUTO_INCREMENT,
  `customer_id` int(11) DEFAULT NULL,
  `coverage` varchar(45) NOT NULL,
  `internal` int(11) DEFAULT NULL,
  `customer_revenue` decimal(32,16) NOT NULL,
  `customer_cost` decimal(32,16) NOT NULL,
  `customer_cost_wo_discount` decimal(32,16) NOT NULL,
  `customer_PNL` decimal(32,16) NOT NULL,
  `total_revenue` decimal(32,16) NOT NULL,
  `total_cost` decimal(32,16) NOT NULL,
  `total_cost_wo_discount` decimal(32,16) NOT NULL,
  `total_PNL` decimal(32,16) NOT NULL,
  `exchange` varchar(45) NOT NULL,
  `customer_proxy_id` int(11) DEFAULT NULL,
  `company_name` varchar(45) NOT NULL,
  `persona_type` varchar(45) NOT NULL,
  `trade_date` datetime NOT NULL,
  `side` enum('buy','sell') NOT NULL,
  `token_type` varchar(20) NOT NULL,
  `base_token` varchar(20) NOT NULL,
  `quote_token` varchar(20) NOT NULL,
  `fees_token` varchar(20) NOT NULL,
  `is_maker` tinyint(1) DEFAULT NULL,
  `rebate_token` varchar(20) NOT NULL,
  `subaccount_id` int(11) DEFAULT NULL,
  `product_type_int` int(11) DEFAULT NULL,
  `product_type` varchar(20) NOT NULL,
  `margin_mode` varchar(20) NOT NULL,
  `platform` varchar(20) NOT NULL,
  `product` varchar(20) NOT NULL,
  `total_trades` int(11) DEFAULT NULL,
  `total_orders` int(11) DEFAULT NULL,
  `quantity` decimal(32,16) NOT NULL,
  `volume` decimal(32,16) NOT NULL,
  `trade_size_usd` decimal(32,16) NOT NULL,
  `fees_owed_usd` decimal(32,16) NOT NULL,
  `affiliate_rebate_usd` decimal(32,16) NOT NULL,
  `fees_rebate_usd` decimal(32,16) NOT NULL,
  `fee_total_usd` decimal(32,16) NOT NULL,
  `net_customer_fees_usd` decimal(32,16) NOT NULL,
  `net_savings_usd` decimal(32,16) NOT NULL,
  `customer_fees_without_discount` decimal(32,16) NOT NULL,
  `default_fees_in_usd` decimal(32,16) NOT NULL,
  `pdate` datetime NOT NULL,
  `v_plus` decimal(32,16) NOT NULL,
  `v_minus` decimal(32,16) NOT NULL,
  `e_plus` decimal(32,16) NOT NULL,
  `e_minus` decimal(32,16) NOT NULL,
  `v_minus_n` decimal(32,16) NOT NULL,
  `internal_agg_edge_metrics` decimal(32,16) NOT NULL,
  `customer_revenue_agg_edge_metrics` decimal(32,16) NOT NULL,
  `customer_cost_agg_edge_metrics` decimal(32,16) NOT NULL,
  `customer_cost_wo_discount_agg_edge_metrics` decimal(32,16) NOT NULL,
  `customer_pnl_agg_edge_metrics` decimal(32,16) NOT NULL,
  `total_revenue_agg_edge_metrics` decimal(32,16) NOT NULL,
  `total_cost_gg_edge_metrics` decimal(32,16) NOT NULL,
  `total_cost_wo_discount_agg_edge_metrics` decimal(32,16) NOT NULL,
  `total_pnl_agg_edge_metrics` decimal(32,16) NOT NULL,
  `token_type_agg_edge_metrics` decimal(32,16) NOT NULL,
  `t_create` datetime NOT NULL DEFAULT CURRENT_TIMESTAMP,
  `t_update` datetime NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  `t_delete` datetime DEFAULT NULL,
  PRIMARY KEY (`id`)
) ENGINE=InnoDB DEFAULT CHARSET=latin1;


CREATE TABLE `ae_market_making_contracts` (
  `id` int(11) NOT NULL AUTO_INCREMENT,
  `particulars` varchar(45) NOT NULL,
  `mm_contract` varchar(45) NOT NULL,
  `counterparty_name` varchar(45) NOT NULL,
  `counterparty_code` varchar(45) NOT NULL,
  `mm_token_name` varchar(10) NOT NULL,
  `nature_of_mm_contract` varchar(45) NOT NULL,
  `period_of_contract` int(11) DEFAULT NULL,
  `contract_start_date` datetime NOT NULL,
  `contract_end_date` datetime NOT NULL,
  `token_quantity` decimal(32,16) NOT NULL,
  `strike_price_of_contract` decimal(32,16) NOT NULL,
  `quantity_sold_cumulative` decimal(32,16) NOT NULL,
  `price_of_quantity_sold_average` decimal(32,16) NOT NULL,
  `gain_loss_on_sale_of_tokens` decimal(32,16) NOT NULL,
  `remaining_mm_tokens` decimal(32,16) NOT NULL,
  `24_hrs_liquidity_of_mm_token` decimal(32,16) NOT NULL,
  `has_contract_terms_revised` enum('yes','no') NOT NULL,
  `gain_loss_on_revision_of_contract` decimal(32,16) NOT NULL,
  `eop_price` decimal(32,16) NOT NULL,
  `notional_gain_loss_based_on_eop_price` decimal(32,16) NOT NULL,
  `token_received_date` datetime NOT NULL,
  `transaction_hash_token_received` varchar(45) NOT NULL,
  `token_return_date` datetime NOT NULL,
  `transaction_hash_token_sent` varchar(45) NOT NULL,
  `t_create` datetime NOT NULL DEFAULT CURRENT_TIMESTAMP,
  `t_update` datetime NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  `t_delete` datetime DEFAULT NULL,
  PRIMARY KEY (`id`)
) ENGINE=InnoDB DEFAULT CHARSET=latin1;


CREATE TABLE `ae_market_making_realized_gain_loss` (
  `id` int(11) NOT NULL AUTO_INCREMENT,
  `particulars` varchar(45) NOT NULL,
  `mm_contract` varchar(45) NOT NULL,
  `counterparty_name` varchar(45) NOT NULL,
  `counterparty_code` varchar(45) NOT NULL,
  `mm_token_name` varchar(10) NOT NULL,
  `nature_of_mm_contract` varchar(45) NOT NULL,
  `period_of_contract` int(11) DEFAULT NULL,
  `contract_start_date` datetime NOT NULL,
  `contract_end_date` datetime NOT NULL,
  `token_quantity` decimal(32,16) NOT NULL,
  `strike_price_of_contract` decimal(32,16) NOT NULL,
  `quantity_sold` decimal(32,16) NOT NULL,
  `price_of_quantity_sold` decimal(32,16) NOT NULL,
  `gain_loss_on_sale_of_tokens` decimal(32,16) NOT NULL,
  `t_create` datetime NOT NULL DEFAULT CURRENT_TIMESTAMP,
  `t_update` datetime NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  `t_delete` datetime DEFAULT NULL,
  PRIMARY KEY (`id`)
) ENGINE=InnoDB DEFAULT CHARSET=latin1;